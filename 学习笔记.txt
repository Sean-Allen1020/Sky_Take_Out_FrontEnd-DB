后端笔记：

common 存放公共类，可以供其它模块使用
pojo     存放entity实体，DTO数据传输对象，VO视图对象
server 存放配置文件，配置类，拦截器，controller，service，mapper，启动类等

swagger创建：
	- 首先pom文件里引入knife4j坐标
	- 创建配置类，该项目中为 WebMvcConfiguration ，类中的 docket 方法，就是knife4j相关配置
	- 再设置静态资源映射
  以上swagger创建流程和代码是固定的，只需要复制代码即可
	